spring:
  datasource:
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
        globally_quoted_identifiers: true
        show_sql: false

  flyway:
    baseline-on-migrate: true
